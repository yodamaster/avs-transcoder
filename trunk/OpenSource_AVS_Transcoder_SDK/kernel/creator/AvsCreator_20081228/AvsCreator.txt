操作说明：

    在命令行方式中不输入任何参数即可获得使用说明。
    命令行参数解释如下：
	-c 创建一个新的asm文件或者向已有的.asm文件中新增一个音/视频轨道。本参数用于指定新增轨道的输入文件名(.avs/.mp3)
	-e 提取一个轨道
	-H 为一个轨道打入提示轨道
	-l 列出.asm文件中所有轨道的信息
	-m 指定网络有效载荷单元的大小
	-r 指定视频帧率
	-t 指定时间度量单位，默认为90000，即默认以1/90000秒为时间单位
	-O 优化文件存储格式（音视频交织）



